# Before
搜索就是一种遍历的方法，再搜索中解决问题。
# 箴言
- 暴力出奇迹，打表过样例
- 当你不会做的时候，试试画搜索树模拟
- 记得判断与回溯！
- 仔细读题
# Learn
## DFS
$\texttt{Deepth First Search}$

核心思想就是：

> 不撞南墙不回头

“回头”就是 `return`。

框架：
```cpp
int ans[N],vis[N];
void dfs(int dep){
    if(边界条件) return;
    进行某种操作
    for(遍历邻居){
        if(vis[i]) continue;
        vis[i] = ans[dep] = i;
        dfs(i);
        vis[i] = ans[dep] = 0;
    }
}
```
### P1706 全排列
__Problem__  
给定 $n$，输出从 $1$ 到 $n$ 的数字的全排列，场宽为 $5$。

__Solve__  
首先要输入 $n$：
```cpp
int n; cin >> n;
```

然后，考虑算法。  
这里因为 $n$ 不确定，所以不能使用枚举，即考虑搜索，`dfs(int cnt)`，其中，`cnt` 为当前枚举的数字个数，如果 $cnt > n$ 就进行输出。  
所以现在考虑输出函数 `print`，这样就要使用数组 `ans` 来存储答案，方便输出，也就是从 $ans_1$ 输出到 $ans_n$ 即可，保留场宽可以用 $\texttt{setw(5)}$ 或 $\texttt{printf("%5d",ans[i])}$  
通过手画递归树可知，对于之前已经用过的数字不应该使用，所以要用一个数组 `vis` 来记录是否被用，并且每次都要从 $1$ 开始搜。  
```text
例如 dfs(3,1)
dfs(1,1)->dfs(2,2)[vis:1,1,0]->dfs(3,3)[vis:1,1,1]->dfs(4,4)[vis:1,1,1]->return cout << "1     2     3" << endl;
                  [vis:1,0,0]<---------[vis:1,1,0]<-+
        +>dfs(3,2)[vis:1,0,1]->dfs(2,3)[vis:1,1,1](这就是为什么要从1开始)
...(剩下的自己脑补吧)
```

在每次搜索完后都要回溯操作，否则那个数字再其他搜索树分支就**不会被使用**。

所以，注意事项：
1) 记得保存答案
2) 记得标记访问
3) 记得回溯重置

```cpp
#include <iostream>
#include <iomanip>//使用 setw 要包含头文件
using namespace std;
const int N = 11;
int n,ans[N],vis[N];
void print(){
    for(int i = 1;i <= n;i++) cout << setw(5) << ans[i];
    cout << endl;//细节
}
void dfs(int cnt){
    if(cnt > n){
        print();
        return;//告辞
    }
    for(int i = 1;i <= n;i++){//每次都从 1 开始查找有哪些没有使用过
        if(vis[i]) continue;//如果使用过就不再用了
        vis[i] = ans[cnt] = i;//一定要记录答案 并标记
        dfs(cnt+1);//传送个数
        vis[i] = ans[cnt] = 0;//回溯 这个ans不重置也行，后面会覆盖掉
    }
}
int main(){
    cin >> n;
    dfs(1);//从第一个开始遍历 因为第一个cnt穿进去必须是1，ans数组才能从1开始记录
    return 0;
}
```
### P1157 组合的输出
__Problem__  
给定 $n$ 个从 $1$ 到 $n$ 的数和一个正整数 $r$，输出 $n$ 个中选 $r$ 的组合，保留 $3$ 个场宽。

~~这简直就是上一道题！~~

__Solve__  
因为 $r$ 不确定，所以用搜索，不用枚举。

我们可以搜索 `dfs(int cnt)` 用来判断是否超了，搜索树仿上题。

还是要储存答案并且判断是否重复，和上题一样，也要回溯。

首先是基本框架：
```cpp
#include <iostream>//I/O 流
#include <iomanip>//setw(3)
using namespace std;
const int N = 24;//实际上这里的最大容量应该是 r 的大小
int n,r;//准备变量
int ans[N],vis[N];
void dfs(int cnt){

}
int main(){
    cin >> n >> r;
    dfs(1);
    return 0;
}
```
这里我们还可以准备一个打印函数 `print` 用于打印 $ans_1$ 到 $ans_r$。
```cpp
void print(){
    for(int i = 1;i <= r;i++) cout << setw(3) << ans[i];
    cout << endl;
}
```
然后就是 `dfs` 里的内容了：
```cpp
void dfs(int cnt){
    if(cnt > r){
        print();//输出
        return;
    }
    for(int i = 1;i <= n;i++){//自己画一下搜索树就知道了
        if(vis[i]) continue;//使用过
        vis[i] = ans[cnt] = i;
        dfs(cnt+1);
        vis[i] = ans[cnt] = 0;
    }
}
```
合并一下：
```cpp
#include <iostream>
#include <iomanip>
using namespace std;
const int N = 24;
int n,r;
int ans[N],vis[N];
void print(){
    for(int i = 1;i <= r;i++) cout << setw(3) << ans[i];
    cout << endl;
}
void dfs(int cnt){
    if(cnt > r){
        print();//超出边界
        return;
    }
    for(int i = 1;i <= n;i++){
        if(vis[i]) continue;//同上一题
        vis[i] = ans[cnt] = i;
        dfs(cnt+1);
        vis[i] = ans[cnt] = 0;
    }
}
int main(){
    cin >> n >> r;
    dfs(1);
    return 0;
}
```
你不会真以为这就行了吧？一测试连样例都过不了。

观察结果发现，有顺序逆过来的，原来组合是考虑顺序的，而不是像上一题一样的排列，所以要仔细读题。

所以我们应该用一个参数来记录当前的位置，每次都往后找，不回头：
```cpp
#include <iostream>
#include <iomanip>
using namespace std;
const int N = 24;
int n,r;
int ans[N],vis[N];
void print(){
    for(int i = 1;i <= r;i++) cout << setw(3) << ans[i];
    cout << endl;
}
void dfs(int now,int cnt){
    if(cnt > r){
        print();//超出边界
        return;
    }
    for(int i = now;i <= n;i++){
        if(vis[i]) continue;//同上一题
        vis[i] = ans[cnt] = i;
        dfs(i,cnt+1);
        vis[i] = ans[cnt] = 0;
    }
}
int main(){
    cin >> n >> r;
    dfs(1,1);
    return 0;
}
```
## BFS
